/**
 * @deprecated Este arquivo não está mais em uso.
 *
 * A estratégia de autenticação unificada do IGLOO-APP usa `lib/supabase.ts`,
 * onde o token JWT do Clerk é injetado via `setSupabaseToken()` a cada 50s
 * pelo `context/AuthContext.tsx`.
 *
 * Este arquivo pode ser removido com segurança.
 */
